apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-82263855-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: ingress-interna
  rules:
  - host: 82263855.ce-aa.kubepia.net
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: app-82263855
            port:
              number: 80
